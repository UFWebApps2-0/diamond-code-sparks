import 'Assignment.js'

/*  Should use these on front end if possible so you 
    don't have to handle/store the assignment objects directly */

    function findAssignment(name) {
        // search from database (returns object of Assignment class)
        return null;
    };
    
    function makeAssignment(name) {
        // create a new Assignment object and push it to the database
    }
    
    function deleteAssignment(name) {
        // find the assignment (use findAssignment)
        // then call delete on it
    }